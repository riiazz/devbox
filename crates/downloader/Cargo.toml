[package]
name = "downloader"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }
flate2 = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
sha2 = "0.10"
tar = "0.4"
thiserror = "2"
toolchain = { path = "../toolchain" }
zip = "2"
