[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "devbox"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
config = { path = "../config" }
runtime = { path = "../runtime" }
workspace = { path = "../workspace" }
