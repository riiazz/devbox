[package]
name = "supervisor"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }
runtime = { path = "../runtime" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.9"
